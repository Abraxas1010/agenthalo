/target/

# Runtime artifacts
/artifacts/*.redb
/artifacts/*.wal
/artifacts/**/*.jsonl
/artifacts/**/*.tmp
/contracts/artifacts/ops/multichain_sepolia/mock_e2e_*.json

# Generated living_agent data (grid cells, embeddings, SNS snapshots)
/artifacts/living_agent/

# Keep deterministic setup fixture tracked
!/artifacts/
!/artifacts/.gitkeep
!/artifacts/kzg/
!/artifacts/kzg/demo-trusted-setup-v1.json

# Lean build artifacts
/.lake/

# Database files (stray test outputs)
*.ndb
*.wal
*.redb
--db
--db.wal

# WIP / internal development notes
/WIP/

# Internal ops accounting (not for public release)
/Docs/ops/
/Docs/integration/

# Sidecar dependencies
/wdk-sidecar/node_modules/

# External skill injections (readonly copies injected at agent launch)
# Identified by .external_source marker file inside each skill directory.
# The sync script and deploy code manage these; they are not repo content.

# OS
.DS_Store
Thumbs.db
__pycache__/
